{% extends 'auth_base.html.twig' %}

{% block title %}Email envoyé{% endblock %}

{% block body %}
<div class="auth-wrapper">
    <div class="container">
        <div class="auth-logo text-center mb-4">
            <h1 class="text-white display-4 mb-0">Blog Kenny</h1>
        </div>
        <div class="card registration-card fade-in mx-auto">
            <div class="card-body">
                <div class="text-center mb-4">
                    <div class="mb-3">
                        <i class="fas fa-envelope-open-text fa-3x text-primary"></i>
                    </div>
                    <h1 class="h2 fw-bold text-primary">Email envoyé</h1>
                </div>

                <div class="alert alert-info d-flex align-items-center" role="alert">
                    <i class="fas fa-info-circle me-2"></i>
                    <div>
                        Si un compte correspond à votre adresse email, un email vient d'être envoyé contenant un lien pour réinitialiser votre mot de passe.
                        Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                    </div>
                </div>

                <div class="alert alert-warning d-flex align-items-center mt-3" role="alert">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    <div>
                        Si vous ne recevez pas d'email, veuillez vérifier vos spams ou 
                        <a href="{{ path('app_forgot_password_request') }}" class="alert-link">réessayer</a>.
                    </div>
                </div>

                <div class="text-center mt-4">
                    <a href="{{ path('app_login') }}" class="text-primary text-decoration-none">
                        <i class="fas fa-arrow-left me-1"></i>
                        Retour à la connexion
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
